const orderServiceController = require("./order-service-controller");
const orderServiceRouter = require("./order-service-router");

module.exports = {
    orderServiceController,
    orderServiceRouter
}
